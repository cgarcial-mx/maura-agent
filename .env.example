# PostgreSQL (Drizzle)
DATABASE_URL=postgres://maura:maura@localhost:5432/maura

# LLM (proveedor se decide después; solo se exige el contrato de privacidad)
# zero-retention, no-training, DPA — ver PRD §13.2 y §18.7
LLM_API_KEY=
LLM_MODEL=
LLM_TIMEOUT_MS=8000

# WhatsApp (Meta Cloud API)
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_VERIFY_TOKEN=

# Portabilidad / borrado self-service (enlace "unsubscribe")
PORTABILITY_SECRET=
PUBLIC_URL=http://localhost:3000
